# MongoDB Connection String
# Replace with your actual MongoDB URI

# For local MongoDB:

MONGODB_URI=mongodb+srv://expencemanage:2026@cluster0.hska33k.mongodb.net/?appName=Cluster0

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/finance-app?retryWrites=true&w=majority

# Instructions:
# 1. If using local MongoDB, install MongoDB and start the server
# 2. If using MongoDB Atlas:
#    - Create account at https://www.mongodb.com/cloud/atlas
#    - Create a cluster
#    - Get connection string and replace above
#    - Make sure to replace <username> and <password> with your credentials
